import React from "react";

const Blog = () => {
  return (
    <div className="h-screen pt-40 sm:pt-40 md:pt-40 p-8">
      <div className="mx-auto flex flex-col gap-6 w-full sm:flex-row">
        <p className="text-2xl text-white">No blogs available at the moment.</p>
      </div>
    </div>
  );
};

export default Blog;
